---
id: 003
title: Tool config, paths, source wrapping and perimeter matcher (pure)
status: done
kind: build
size: m
wave: 1
depends_on: []
touches: [apps/web/lib/tools/config.ts, apps/web/lib/tools/perimeter.ts, apps/web/tests/tools-config.test.ts, apps/web/tests/tools-perimeter.test.ts]
created_by: 002
session: 8ddbbc0b-a47d-48b0-bfb2-fbd7eac35e84
model: opus
effort: xhigh
---

## Task

Create the pure core of the Tools feature under apps/web/lib/tools/ (no React, no prisma, no I/O). Read the plan (.wayfinder/tasks/plan.md) for the frontmatter shape and the connector analogue in apps/web/lib/connectors/{config,perimeter}.ts — mirror their style (return refusal strings, never throw for policy).

**apps/web/lib/tools/perimeter.ts** — export `interface ToolPerimeter { read: string[]; write: string[]; types: string[]; connectors: string[]; agents: string[] }`, `EMPTY_PERIMETER`, `parseToolPerimeter(raw: unknown): { ok: true; perimeter } | { ok: false; error: string }` (accepts the frontmatter `perimeter:` object; every entry a non-empty string; note globs are context-relative POSIX paths, `*` matches within a segment, `**` across segments, an entry ending in `/` means that folder and everything below; connectors/agents/types allow `*` and `prefix-*`), `globMatch(pattern, path): boolean`, `refuseRead(p, notePath)`, `refuseWrite(p, notePath)` (write implies nothing about read; both must be declared), `refuseType(p, typeName)`, `refuseConnector(p, name)`, `refuseAgent(p, name)` — each returns `null` when allowed or a refusal string in the connector style (`tool perimeter denied: deals/x.md is not in this tool's read globs (…)`), `perimeterIsEmpty(p)`, `diffPerimeter(prev, next): { read: {added, removed}, write: …, types, connectors, agents }`, `describePerimeter(p): string[]` (human bullet lines for review UIs).

**apps/web/lib/tools/config.ts** — export `TOOLS_DIR = 'tools'`, `TOOL_NAME_RE = /^[a-z0-9][a-z0-9-]{0,62}$/`, `toolFolderPath(name)`, `toolIndexPath(name)` = `tools/<name>/index.md`, `toolUiPath(name)` = `tools/<name>/ui.md`, `toolDataPath(name)` = `tools/<name>/data.md`, `toolNameOfPath(path): string|null`, `toolFileKindOfPath(path): 'index'|'ui'|'data'|'other'|null` (null when not under tools/), `isToolPath(path)`, `TOOL_SOURCE_FILES = { ui: { path: 'ui.md', authorName: 'ui.tsx', lang: 'tsx' }, data: { path: 'data.md', authorName: 'data.js', lang: 'js' } }`, `wrapSource(code, lang: 'tsx'|'js'): string` (frontmatter `type: tool-source`, `lang: <lang>`, body = one fenced block using a fence longer than any run of backticks in the code), `unwrapSource(md): { code: string; lang: 'tsx'|'js' } | null`, `parseToolConfig(fm: NoteFrontmatter, name: string): { ok: true; config: ToolConfig } | { ok: false; error: string }` where `interface ToolConfig { name; title; description; version: number; surfaces: { rail: { label: string; icon: string } | null; types: { type: string; mode: 'page'|'tab' }[] }; perimeter: ToolPerimeter }` (`type: tool` required, case-insensitive; `version` integer ≥ 0 default 0; `surfaces.rail.icon` one of a small named set, default 'grid'; type names normalised lower-case; a Tool may claim `mode: 'page'` only for non-built-in types — built-ins person/space/event/resource/section/channel/connector/agent/tool/index accept `tab` only, else error), and `newToolIndexNote({ name, title, description }): string` producing a valid index note (H1 title, one short paragraph, empty `perimeter:` and `surfaces:` scaffolding in frontmatter, plus a `## How it works` docs section). Import `NoteFrontmatter` from '@/lib/notes/shared/markdown'.

Tests (node:test + node:assert/strict via tsx, see apps/web/tests/connector-isolate.test.ts): glob semantics (`deals/**` matches `deals/a/b.md` and not `dealsx/a.md`; `people/*/index.md`; trailing-slash folder form; `**` alone matches everything), each refuse* both ways, diff, wrap/unwrap round-trip including code containing ``` fences, config parse happy path + every error, path helpers.

Acceptance: `pnpm --filter @visvine/web exec tsc --noEmit`, `pnpm lint`, `pnpm test` pass; knip stays zero (only export what will be consumed — later tasks import these names exactly, so keep them all).

## Outcome

Built the pure core of the Tools feature: lib/tools/perimeter.ts (glob/name matcher + gates + diff + review bullets) and lib/tools/config.ts (paths, source wrapping, frontmatter parser, starter note), plus 44 node:test assertions across tests/tools-perimeter.test.ts and tests/tools-config.test.ts. All 44 pass; my four files typecheck and lint clean and knip reports nothing against them.

perimeter.ts exports exactly the named API: ToolPerimeter, EMPTY_PERIMETER (deep-frozen, so the shared default can't be widened in-process), parseToolPerimeter, globMatch, refuseRead/refuseWrite/refuseType/refuseConnector/refuseAgent, perimeterIsEmpty, diffPerimeter, describePerimeter. Gates return null-or-refusal in the connector's voice ("tool perimeter denied: salaries/x.md is not in this tool's read globs (deals/**)"), with a distinct "declares no read globs" message for the empty case; read and write are wholly independent. Glob grammar: `*` within a segment, `**` only as a whole segment (trailing = everything below, middle = any depth including none), trailing `/` = that folder and below, leading slashes normalised, `?` deliberately not a wildcard; compiled regexes are cached. Type/connector/agent lists accept `*` and `prefix-*`, matched case-insensitively; type names are normalised lower-case so they line up with surfaces.types. diffPerimeter compares entries literally — `deals/` vs `deals/**` shows as a change rather than being hidden from an upgrade review.

config.ts exports TOOLS_DIR, TOOL_NAME_RE, toolFolderPath/toolIndexPath/toolUiPath/toolDataPath, toolNameOfPath, toolFileKindOfPath, isToolPath, TOOL_SOURCE_FILES, wrapSource/unwrapSource, ToolConfig, parseToolConfig, newToolIndexNote, plus TOOL_RAIL_ICONS (the named icon set the rail UI will need; default 'grid'). wrapSource picks a fence one backtick longer than the longest run in the code, so source containing its own ``` fences round-trips; unwrapSource treats frontmatter `lang:` as authoritative over the fence info string. parseToolConfig requires `type: tool` (case-insensitive), defaults title→name / description→'' / version→0 (integer ≥ 0 else error), reads surfaces.rail (bare `{}` → label from title, icon 'grid') and surfaces.types (a bare string means mode tab), rejects `mode: page` for built-ins — the ten named types plus every legacy spelling entityKindOf still resolves ('community', 'organization', 'people'). newToolIndexNote emits H1 + one paragraph + "## How it works", with every perimeter/surfaces key present and empty, and round-trips through parseToolConfig.

Two deviations worth recording. (1) The task said to import NoteFrontmatter from '@/lib/notes/shared/markdown'; that module only imports the type, so I import it from '@/lib/notes/shared/types', as lib/connectors/config.ts and lib/agents/config.ts do. (2) Task 004 has meanwhile added toolNameOfEntityPath / isToolIndexPath to lib/notes/entities.ts, which overlaps my toolNameOfPath / toolIndexPath (mine also validates TOOL_NAME_RE, and knip currently flags theirs as unused) — wave 2 should consume one pair and drop the other; I left entities.ts untouched.

Verification: `node --import tsx --test tests/tools-config.test.ts tests/tools-perimeter.test.ts` → 44/44 pass. `eslint` on the four files → clean (full `pnpm lint` was also clean when I ran it). Full-repo state is not green, entirely from other wave-1 agents' in-flight files, none of it touching my scope: `tsc --noEmit` reports 3 errors (features/tools/kit/runtime.ts:224-225, lib/tools/compile.ts:299) — my earlier full run before those files landed was clean and no error names my files; `pnpm test` is 657/659 with feature-access.test.ts ("hides notes and events from the nav rail") and node-type-registry.test.ts failing because the new `tools` feature key isn't yet in those tests' expectations (task 004's work); knip lists features/tools/kit/*, lib/tools/protocol.ts and esbuild as unused, again other tasks' files awaiting consumers.
