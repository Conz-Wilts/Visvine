---
id: 019
title: "Author surfaces: `/directory/tool:<name>` Tool tab and `/tools/preview/[name]`"
status: done
kind: build
size: m
wave: 3
depends_on: [015, 014]
touches: [apps/web/features/profile/components/ToolPageContent.tsx, "apps/web/app/(auth)/directory/[nodeId]/page.tsx", "apps/web/app/(auth)/tools/preview/[name]/page.tsx", apps/web/features/tools/components/ToolPreview.tsx]
created_by: 002
session: ff54d6ac-3e9c-409d-bd72-546d26ade6ae
model: opus
effort: xhigh
---

## Task

1. **NodeRoute** (app/(auth)/directory/[nodeId]/page.tsx): add a `tool:` branch → `ToolRoute` rendering `NodePage` with first tab `{ id: 'about', label: 'Tool' }` and body `ToolPageContent`, exactly like `AgentRoute` (every member sees the tab; Context/Raw remain, and the sub-notes `ui.md`/`data.md` open through the entity Context tab's notes strip). Keep the diff to that branch — the type-page task will touch this file next wave.
2. **features/profile/components/ToolPageContent.tsx** (mirror ConnectorPageContent/AgentPageContent structure): fetch `/api/communities/<spaceId>/tools/authoring/<name>`; show build status card (ok / diagnostics list with file:line:col), config summary (title, version, surfaces), `PerimeterSummary`, requirements in THIS space, files list (index.md, ui.tsx, data.js → links to the Context tab sub-notes: `entityContextHref(nodeId, 'ui.md')`), publish status/history, actions: 'Preview' (→ `/tools/preview/<name>`), 'Publish' (admins → POST authoring publish; confirm dialog with perimeter), 'Copy MCP hint'. Not-found and forbidden states.
3. **app/(auth)/tools/preview/[name]/page.tsx** + **ToolPreview.tsx**: renders `<ToolFrame target={{ kind: 'preview', spaceId: currentSpace.id, name }} mode="preview"/>` in the main content area with a slim header strip (tool title, build status, 'Reload', 'Open source'); on build errors the frame document already shows the diagnostics card. This is the target of the `visvine-desktop://open/tools/preview/<name>` deep link — nothing desktop-side to change (deepLinkToPath is generic); confirm by reading apps/desktop/src/urls.ts. Acceptance: tsc/lint/knip clean; visit both pages against `pnpm dev` with a scaffolded tool (report).

## Outcome

Both author surfaces are live: `/directory/tool:<name>` gains a first "Tool" tab beside Context/Raw, and `/tools/preview/<name>` runs an author's working copy in a sandboxed frame. tsc, `eslint --max-warnings=0` and 943/943 tests pass; knip is down to two pre-existing entries, neither mine. Driven end to end in a browser against `next dev` with a scaffolded Tool, as admin and as a member, with a working build and a deliberately broken one.

**The Tool tab.** `NodeRoute` gained a `tool:` branch → `ToolRoute` → `NodePage` with `{ id: 'about', label: 'Tool' }` and `ToolPageContent`, exactly like `AgentRoute` — every member gets the tab (members author Tools, and the authoring route is grant-gated, not admin-gated), Context/Raw stay, and `ui.md`/`data.md` open as sub-notes of the entity folder. `ToolPageContent` (mirroring ConnectorPageContent's flat rule-separated sections) shows: a status verdict (Ready / Runs degraded / Not building / Never built / Not usable), the build card (`ui.tsx:3:0 message` lines + warnings + config error, or "Compiles — 606 B of bundle, built 3m ago"), the config read back, `PerimeterSummary`, the requirements check for THIS space, the three files linking to `entityContextHref(nodeId, 'ui.md')`, and the publication trail. Actions: Preview, Copy MCP hint (a paste-able line naming the space id, the tool name, `get_tool_sdk` → `read_tool` → `write_tool` → `check_tool` and the preview URL), and Publish for admins only — a confirm dialog showing the exact reach every installing space would be granting, a reviewer note, disabled while the build is red or a version is already pending. Not-found and forbidden are separate states (404 says the same thing about "deleted" and "never shared with you" on purpose).

**The preview.** `ToolPreview` renders a slim strip (title, name, build chip, "preview · working copy", Reload, Open source) over `<ToolFrame target={{kind:'preview',…}} mode="preview"/>`. Reload re-keys the frame, so it re-mints the token and remounts from scratch, and re-fetches the build so the chip is as fresh as the frame. Confirmed in `apps/desktop/src/urls.ts`: `deepLinkToPath` maps `visvine-desktop://open/tools/preview/<name>` to this path generically — nothing desktop-side to change.

**Two things I had to fix that the task assumed worked.**
1. *The task's premise that "the frame document already shows the diagnostics card" does not hold.* The runtime route does render one, but `ToolFrame` covers any frame that hasn't handshaked with a skeleton and, 15s later, replaces the whole slot with a generic "did not finish loading" card — so the compile errors are never read. Verified live. On the author's own preview I answer it here instead (the build row is right there, and there is no bundle to mint a token for), sharing a new `BuildDiagnostics` component with the Tool tab so the two never word a defect differently. The host defect itself matters for INSTALLED Tools, where the frame is the only surface — filed as **task 047**.
2. *In-app links to `?tab=context` did nothing.* `NodePage` seeded its tab from the URL once, so a same-route `<Link>` moved the address bar and left the page on the first tab — my Files links, and the agent page's existing "edit the brief" link, both silently failed. Fixed with a URL→state effect compared against the last URL value rather than the current tab, so a user's own tab click isn't dragged back while `router.replace` lands (verified both directions, no bounce). The three sibling shells in that file have the same latent bug and were left alone per the "keep the diff to that branch" instruction — **task 049**.

**Two edits outside the listed scope, both because the right file was leased.** `GET …/tools/authoring/<name>` now answers `{ tool, requirements, versions }`: nothing anywhere returned a working copy's requirements or its publication trail, and both are explicit deliverables. `lib/tools/installs.ts#spaceFacts` (task 017) and `lib/tools/api.ts` (task 020) were both held, so the requirements walk landed as `toolRequirementsInSpace` in `lib/tools/service.ts` (the working-copy half of the feature — a defensible home, read under the AUTHOR's principal like `check_tool`) and the envelope as `AuthoredToolView` in the same file with a TODO. That also means `ToolPageContent` fetches directly rather than through `features/tools/lib/client.ts#fetchAuthoredTool`, which is typed to the narrower envelope. **Task 048** folds all of it back and collapses what are now three readings of "what does this space have" (installs.ts, appTools.ts, service.ts) — they must never disagree. `knip.json` dropped its `PerimeterSummary` exemption, which now has two consumers.

**Live walkthrough** — the shared `next dev` on :3000 (a second server is refused in the same directory; the existing one is another agent's), dev-login cookies for `admin@local.dev` and `member@local.dev`, against `community:blackbird-ventures` with a scratch Tool `preview-smoke-019` whose perimeter names `person` (satisfied) and `no-such-connector` (not). REST: the envelope carries all three keys, requirements reads `{connectors:["no-such-connector"],types:[],agents:[]}`, versions `[]`; member → 404 (grant-narrowed), unknown tool → 404, anonymous → 401. UI: the Tool tab renders every section correctly; `data.js` and `ui.tsx` links open the right sub-note through the Context tab's notes strip with the tree highlighting it; Tool↔Context switches both ways without bouncing; Copy MCP hint flips to "Copied"; the preview renders the Tool live in `sandbox="allow-scripts"` and its button increments; Reload resets it; a broken `ui.tsx` turns the chip red, disables Publish, lists `ui.tsx:3:0 The character "}" is not valid inside a JSX element` on both surfaces, and restoring the source brings the preview straight back; `/tools/preview/no-such-tool-019` shows "Nothing to preview"; the member sees the tab with the not-found body. Everything was deleted from the shared dev DB afterwards (3 notes, 1 folder, 1 build row, the `tool:preview-smoke-019` node) and re-read as empty. Scratch scripts stayed in the task scratch dir; screenshots were removed from `.playwright-mcp/`.

**Note for whoever hits it next:** the shared dev server 500s in bursts with "Jest worker encountered 2 child process exceptions" while several agents compile against it. It recovers on its own within a minute or two — it is not a code fault, and `tsc`/`eslint` stay clean through it.
