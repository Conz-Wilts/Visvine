// The wire protocol lives in @visvine/tool-protocol, shared by the server, the
// host, the kit and the CLI; this path is where the app has always imported it.
export * from '@visvine/tool-protocol/protocol'
