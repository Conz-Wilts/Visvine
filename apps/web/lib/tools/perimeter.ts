// The permission grammar lives in @visvine/tool-protocol, shared by the server,
// the review and install surfaces, and the CLI; this path is where the app has
// always imported it.
export * from '@visvine/tool-protocol/perimeter'
