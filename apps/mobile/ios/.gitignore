# XcodeGen output (regenerate with `xcodegen generate`)
*.xcodeproj
*.xcworkspace

# Xcode / build
DerivedData/
build/
*.xcuserstate
xcuserdata/
.DS_Store

# SwiftPM
.swiftpm/
Packages/

# Developer-local signing/config overrides (Team ID, etc) — never commit.
Config/Local.xcconfig
