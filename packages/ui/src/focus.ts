/**
 * The one keyboard-focus mark every control draws: a soft ring in the accent,
 * shown for keyboard focus only (`focus-visible`), never on a click.
 */
export const FOCUS_RING = 'focus:outline-none focus-visible:ring-2 focus-visible:ring-accent/40';
