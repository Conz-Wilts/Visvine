{
  "name": "@visvine/tool-protocol",
  "version": "0.1.0",
  "private": true,
  "description": "A Visvine Tool's contract — the wire protocol, the manifest, the permission grammar and bindings — pure, for the server, the host, the kit and the CLI",
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./protocol": "./src/protocol.ts",
    "./perimeter": "./src/perimeter.ts",
    "./manifest": "./src/manifest.ts",
    "./bindings": "./src/bindings.ts",
    "./reach": "./src/reach.ts",
    "./dependencies": "./src/dependencies.ts"
  },
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "@visvine/config": "workspace:*",
    "typescript": "^5"
  }
}
